<?php
/** @var \Custom\SearchExtended\Block\Index\Vendors $block */
$vendors = $block->getSearchCollection();

// Create pager HTML dynamically
$pager = $block->getPagerHtml();
?>

<style>
  #vendors {
    padding-top: 20px;
  }

  body {
    background: #f9f9fb;
    color: #333;
    font-size: 14px;
  }

  .vendor-card {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 16px;
    width: 100%;
    padding: 20px;
    margin-bottom: 15px;
    background: #fff;
    border-radius: 10px;
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
    text-decoration: none !important;
    color: inherit;
  }

  .vendor-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
  }

  .vendor-left {
    display: flex;
    align-items: flex-start;
    gap: 15px;
    min-width: 0;
    flex: 1 1 auto;
  }

  .vendor-logo {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    overflow: hidden;
    flex: 0 0 50px;
  }

  .vendor-logo img {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  .vendor-info {
    min-width: 0;
    flex: 1 1 auto;
  }

  .vendor-info h3 {
    margin-bottom: 3px;
    font-size: 18px;
    line-height: 1.3;
    word-break: break-word;
  }

  .vendor-meta {
    margin-bottom: 8px;
    font-size: 13px;
    color: gray;
  }

  .vendor-desc {
    max-width: 550px;
    font-size: 13px;
    color: #555;
    line-height: 1.5;
    word-break: break-word;
  }

  .vendor-right {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 20px;
    flex: 0 0 auto;
    flex-wrap: wrap;
  }

  .vendor-country {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 13px;
  }

  .vendor-country img {
    width: 18px;
    height: 12px;
  }

  .vendor-right .visit-link {
    padding: 6px 12px;
    background: #f4f6ff;
    border-radius: 6px;
    color: #2d62f0;
    font-size: 13px;
    font-weight: 500;
    white-space: nowrap;
  }

  .pager {
    margin-top: 20px;
    text-align: center;
  }

  .pager .pages a,
  .pager .pages span {
    display: inline-block;
    margin: 0 4px;
    padding: 6px 12px;
    background: #fff;
    border: 1px solid #ddd;
    border-radius: 6px;
    color: #333;
    font-size: 13px;
    text-decoration: none;
  }

  .pager .pages .current {
    background: #2d62f0;
    border-color: #2d62f0;
    color: #fff;
  }

  @media (max-width: 768px) {
    #vendors {
      padding-top: 12px;
    }

    .vendor-card {
      flex-direction: column;
      align-items: stretch;
      gap: 12px;
      padding: 16px;
    }

    .vendor-left {
      width: 100%;
      gap: 12px;
    }

    .vendor-info h3 {
      font-size: 17px;
    }

    .vendor-meta,
    .vendor-desc {
      font-size: 12px;
    }

    .vendor-right {
      width: 100%;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
    }

    .vendor-country {
      min-width: 0;
      flex-wrap: wrap;
    }

    .vendor-right .visit-link {
      padding: 8px 12px;
      font-size: 12px;
    }

    .pager .pages a,
    .pager .pages span {
      margin: 2px;
      padding: 6px 10px;
    }
  }
</style>

<?php if ($vendors && $vendors->getSize() > 0): ?>
  <div id="vendors" class="tab-content active">
    <?php foreach ($vendors as $vendor): ?>
      <?php
        $country = $vendor->getData('country_id');
        $category = $vendor->getData('business_category');
        $website = $vendor->getData('website');
        $vendorUrl = $block->getBaseUrl() . 'shop/' . $vendor->getData('vendor_id');
        $logo = $vendor->getData('upload_logo');
        $storemediaUrl = $block->getBaseUrl(\Magento\Framework\UrlInterface::URL_TYPE_MEDIA) . "pub/media/";
        $mediaUrl = preg_replace('#/(en|ar|default)(/|$)#i', '/', $storemediaUrl);
        $mediaUrl = $logo ? $mediaUrl . $logo : '';
      ?>
      <a href="<?= $block->escapeUrl($vendorUrl) ?>" class="vendor-card">
        <div class="vendor-left">
          <div class="vendor-logo">
            <?php if ($logo): ?>
              <img src="<?= $block->escapeUrl($mediaUrl); ?>" alt="<?= $block->escapeHtml($block->getVendorName($vendor)); ?>">
            <?php endif; ?>
          </div>
          <div class="vendor-info">
            <h3><?= ucfirst($block->escapeHtml($block->getVendorName($vendor))); ?></h3>
            <div class="vendor-meta"><?= $block->escapeHtml($category); ?></div>
            <div class="vendor-desc"><?= $block->escapeHtml($block->getBusinessDescriptions($vendor)); ?></div>
          </div>
        </div>
        <div class="vendor-right">
          <div class="vendor-country">
            <img class="country-flag" src="https://flagcdn.com/<?= strtolower($block->escapeHtml($country ?: 'N/A')) ?>.svg" alt="<?= strtolower($block->escapeHtml($country ?: 'N/A')) ?>">
            <?= $block->escapeHtml($country); ?>
          </div>
          <span class="visit-link"><?= __("Visit Website") ?> -></span>
        </div>
      </a>
    <?php endforeach; ?>

    <div class="pager">
      <?= $pager ?>
    </div>
  </div>
<?php else: ?>
  <div class="p-6 text-center text-gray-600">
    <p><?= $block->escapeHtml(__("We can't find vendors matching the selection.")) ?></p>
  </div>
<?php endif; ?>

<?php if ($vendors && $vendors->getSize() > 0): ?>
  <style>
    .message.notice {
      display: none;
    }
  </style>
<?php endif; ?>
