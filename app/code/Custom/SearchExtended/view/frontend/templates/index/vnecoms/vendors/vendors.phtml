<?php
/** @var \Custom\SearchExtended\Block\Index\Vendors $block */
$vendors = $block->getSearchCollection();

// Create pager HTML dynamically
$pager = $block->getPagerHtml();
?>

<style>
  #vendors {
    padding-top: 20px;
  }

  body {
    background: #f9f9fb;
    color: #333;
    font-size: 14px;
  }

  .vendor-card {
    display: flex;
    flex-direction: column;
    gap: 14px;
    width: 100%;
    padding: 20px;
    margin-bottom: 15px;
    background: #fff;
    border-radius: 12px;
    box-shadow: 0 2px 10px rgba(15, 23, 42, 0.06);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
    text-decoration: none !important;
    color: inherit;
  }

  .vendor-card__top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    width: 100%;
    min-width: 0;
  }

  .vendor-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 18px rgba(15, 23, 42, 0.09);
  }

  .vendor-left {
    display: flex;
    align-items: flex-start;
    gap: 15px;
    min-width: 0;
    flex: 1 1 auto;
  }

  .vendor-logo {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    overflow: hidden;
    flex: 0 0 50px;
  }

  .vendor-logo img {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  .vendor-info {
    min-width: 0;
    flex: 1 1 auto;
  }

  .vendor-info h3 {
    margin-bottom: 3px;
    font-size: 18px;
    line-height: 1.3;
    word-break: break-word;
  }

  .vendor-meta {
    margin-bottom: 8px;
    font-size: 13px;
    color: gray;
  }

  .vendor-desc {
    margin-left: 65px;
    max-width: 720px;
    font-size: 13px;
    color: #555;
    line-height: 1.5;
    word-break: break-word;
  }

  .vendor-right {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 20px;
    flex: 0 0 auto;
    flex-wrap: wrap;
  }

  .vendor-country {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 13px;
  }

  .vendor-country img {
    width: 18px;
    height: 12px;
  }

  .vendor-right .visit-link {
    padding: 6px 12px;
    background: #f4f6ff;
    border-radius: 6px;
    color: #2d62f0;
    font-size: 13px;
    font-weight: 500;
    white-space: nowrap;
  }

  .vendor-results-pager {
    margin-top: 48px;
    padding-top: 24px;
    border-top: 1px solid #e5e7eb;
    width: 100%;
    box-sizing: border-box;
  }

  .vendor-results-pager > .pager,
  .vendor-results-pager > .pager--search-inner {
    display: flex !important;
    align-items: center !important;
    justify-content: space-between !important;
    gap: 24px !important;
    width: 100% !important;
    margin: 0 !important;
    padding: 0 !important;
    box-sizing: border-box !important;
    flex-wrap: nowrap !important;
    clear: both !important;
  }

  .vendor-results-pager > .pager .toolbar-amount,
  .vendor-results-pager > .pager .pages-label {
    display: none !important;
  }

  .vendor-results-pager > .pager .limiter {
    order: 1 !important;
    display: inline-flex !important;
    align-items: center !important;
    gap: 16px !important;
    margin: 0 !important;
    min-width: 0 !important;
    white-space: nowrap !important;
    flex: 0 0 auto !important;
  }

  .vendor-results-pager > .pager .limiter-label {
    color: #1f2937 !important;
    font-size: 18px !important;
    font-weight: 500 !important;
    line-height: 1.2 !important;
  }

  .vendor-results-pager > .pager .limiter-options {
    appearance: auto !important;
    -webkit-appearance: auto !important;
    -moz-appearance: auto !important;
    min-width: 96px !important;
    height: 56px !important;
    padding: 0 18px !important;
    border: 1px solid #dbe3f0 !important;
    border-radius: 14px !important;
    background: #fff !important;
    color: #1f2937 !important;
    font-size: 18px !important;
    font-weight: 500 !important;
    outline: none !important;
  }

  .vendor-results-pager > .pager .pages {
    order: 2 !important;
    margin-left: auto !important;
    width: auto !important;
    float: none !important;
    display: flex !important;
    justify-content: flex-end !important;
    align-items: center !important;
    min-width: 0 !important;
  }

  .vendor-results-pager > .pager .pages-items {
    display: flex !important;
    align-items: center !important;
    justify-content: flex-end !important;
    gap: 8px !important;
    list-style: none !important;
    margin: 0 !important;
    padding: 0 !important;
    flex-wrap: nowrap !important;
    white-space: nowrap !important;
  }

  .vendor-results-pager > .pager .item {
    margin: 0 !important;
    padding: 0 !important;
    flex: 0 0 auto !important;
  }

  .vendor-results-pager > .pager .page,
  .vendor-results-pager > .pager .action,
  .vendor-results-pager > .pager .disabled {
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
    box-sizing: border-box !important;
    min-height: 56px !important;
    border: 1px solid #dbe3f0 !important;
    border-radius: 12px !important;
    background: #fff !important;
    color: #6b7280 !important;
    font-size: 16px !important;
    font-weight: 500 !important;
    text-decoration: none !important;
    white-space: nowrap !important;
    line-height: 1 !important;
  }

  .vendor-results-pager > .pager .page {
    width: 56px !important;
    min-width: 56px !important;
    padding: 0 !important;
  }

  .vendor-results-pager > .pager .action,
  .vendor-results-pager > .pager .disabled {
    min-width: 132px !important;
    padding: 0 18px !important;
    gap: 10px !important;
    background: #f8fafc !important;
  }

  .vendor-results-pager > .pager .pages-item-previous a,
  .vendor-results-pager > .pager .pages-item-previous .disabled,
  .vendor-results-pager > .pager .pages-item-next a,
  .vendor-results-pager > .pager .pages-item-next .disabled {
    min-width: 152px !important;
    padding: 0 20px !important;
    gap: 10px !important;
  }

  .vendor-results-pager > .pager .item.current .page {
    background: #1f3f75 !important;
    border-color: #1f3f75 !important;
    color: #fff !important;
  }

  .vendor-results-pager > .pager .page .label,
  .vendor-results-pager > .pager .action .label,
  .vendor-results-pager > .pager .disabled .label {
    display: none !important;
  }

  .vendor-results-pager > .pager .pages-item-previous a::before,
  .vendor-results-pager > .pager .pages-item-previous .disabled::before {
    content: "\2039" !important;
    font-size: 24px !important;
    line-height: 1 !important;
    font-weight: 400 !important;
    color: inherit !important;
  }

  .vendor-results-pager > .pager .pages-item-next a::after,
  .vendor-results-pager > .pager .pages-item-next .disabled::after {
    content: "\203A" !important;
    font-size: 24px !important;
    line-height: 1 !important;
    font-weight: 400 !important;
    color: inherit !important;
  }

  .vendor-results-pager > .pager .pages-item-previous .disabled,
  .vendor-results-pager > .pager .pages-item-next .disabled {
    opacity: 1 !important;
    color: #8b93a5 !important;
    border-color: #dbe3f0 !important;
    background: #f8fafc !important;
  }

  @media (max-width: 768px) {
    #vendors {
      padding-top: 10px;
    }

    .vendor-card {
      gap: 10px;
      padding: 14px 14px 16px;
      border-radius: 16px;
    }

    .vendor-card__top {
      width: 100%;
      gap: 12px;
      align-items: flex-start;
    }

    .vendor-left {
      gap: 12px;
      align-items: flex-start;
      min-width: 0;
    }

    .vendor-logo {
      width: 44px;
      height: 44px;
      flex: 0 0 44px;
    }

    .vendor-right {
      gap: 10px;
      align-items: center;
      justify-content: flex-end;
      flex-wrap: nowrap;
      margin-left: auto;
    }

    .vendor-country {
      min-width: 0;
      flex-wrap: nowrap;
      gap: 5px;
      font-size: 12px;
      white-space: nowrap;
    }

    .vendor-country img {
      width: 16px;
      height: 11px;
    }

    .vendor-right .visit-link {
      padding: 8px 14px;
      font-size: 12px;
      border-radius: 8px;
    }

    .vendor-info h3 {
      font-size: 16px;
      line-height: 1.25;
      margin-bottom: 4px;
    }

    .vendor-meta {
      font-size: 12px;
      margin-bottom: 0;
    }

    .vendor-desc {
      margin-left: 0;
      max-width: 100%;
      font-size: 12px;
      line-height: 1.55;
      color: #4b5563;
      display: -webkit-box;
      -webkit-box-orient: vertical;
      -webkit-line-clamp: 3;
      overflow: hidden;
    }

    .vendor-results-pager {
      margin-top: 32px;
      padding-top: 20px;
    }

    .vendor-results-pager > .pager,
    .vendor-results-pager > .pager--search-inner {
      flex-direction: column !important;
      align-items: stretch !important;
      gap: 16px !important;
    }

    .vendor-results-pager > .pager .limiter {
      width: 100% !important;
      justify-content: space-between !important;
    }

    .vendor-results-pager > .pager .limiter-label {
      font-size: 16px !important;
    }

    .vendor-results-pager > .pager .limiter-options {
      min-width: 84px !important;
      height: 44px !important;
      font-size: 15px !important;
    }

    .vendor-results-pager > .pager .pages {
      width: 100% !important;
      justify-content: flex-end !important;
    }

    .vendor-results-pager > .pager .pages-items {
      width: 100% !important;
      flex-wrap: wrap !important;
      justify-content: flex-end !important;
    }

    .vendor-results-pager > .pager .action,
    .vendor-results-pager > .pager .disabled {
      min-width: 112px !important;
      height: 44px !important;
      padding: 0 14px !important;
      font-size: 14px !important;
    }

    .vendor-results-pager > .pager .page {
      width: 44px !important;
      min-width: 44px !important;
      height: 44px !important;
      font-size: 14px !important;
    }
  }
</style>

<?php if ($vendors && $vendors->getSize() > 0): ?>
  <div id="vendors" class="tab-content active">
    <?php foreach ($vendors as $vendor): ?>
      <?php
        $country = $vendor->getData('country_id');
        $category = $vendor->getData('business_category');
        $website = $vendor->getData('website');
        $vendorUrl = $block->getBaseUrl() . 'shop/' . $vendor->getData('vendor_id');
        $logo = $vendor->getData('upload_logo');
        $storemediaUrl = $block->getBaseUrl(\Magento\Framework\UrlInterface::URL_TYPE_MEDIA) . "pub/media/";
        $mediaUrl = preg_replace('#/(en|ar|default)(/|$)#i', '/', $storemediaUrl);
        $mediaUrl = $logo ? $mediaUrl . $logo : '';
      ?>
      <a href="<?= $block->escapeUrl($vendorUrl) ?>" class="vendor-card">
        <div class="vendor-card__top">
          <div class="vendor-left">
            <div class="vendor-logo">
              <?php if ($logo): ?>
                <img src="<?= $block->escapeUrl($mediaUrl); ?>" alt="<?= $block->escapeHtml($block->getVendorName($vendor)); ?>">
              <?php endif; ?>
            </div>
            <div class="vendor-info">
              <h3><?= ucfirst($block->escapeHtml($block->getVendorName($vendor))); ?></h3>
              <div class="vendor-meta"><?= $block->escapeHtml($category); ?></div>
            </div>
          </div>
          <div class="vendor-right">
            <div class="vendor-country">
              <img class="country-flag" src="https://flagcdn.com/<?= strtolower($block->escapeHtml($country ?: 'N/A')) ?>.svg" alt="<?= strtolower($block->escapeHtml($country ?: 'N/A')) ?>">
              <?= $block->escapeHtml($country); ?>
            </div>
            <span class="visit-link"><?= __("Visit Website") ?> -></span>
          </div>
        </div>
        <div class="vendor-desc"><?= $block->escapeHtml($block->getBusinessDescriptions($vendor)); ?></div>
      </a>
    <?php endforeach; ?>

    <div class="vendor-results-pager">
      <?= $pager ?>
    </div>
  </div>
<?php else: ?>
  <div class="p-6 text-center text-gray-600">
    <p><?= $block->escapeHtml(__("We can't find vendors matching the selection.")) ?></p>
  </div>
<?php endif; ?>

<?php if ($vendors && $vendors->getSize() > 0): ?>
  <style>
    .message.notice {
      display: none;
    }
  </style>
<?php endif; ?>
