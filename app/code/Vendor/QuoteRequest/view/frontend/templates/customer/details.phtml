<?php
/** @var \Vendor\QuoteRequest\Block\Customer\Details $block */
$quote = $block->getQuote();
$items = $block->getItems();
$quoteId = (int) $block->getQuoteId();
$statusClass = $block->getStatusClass();
$statusLabel = $block->getStatusLabel();
$message = $block->getMessage();
$supplierName = $block->getSupplierName();
$isCancelable = $block->canCancel();
?>

<div class="rfq-details-page">
    <div class="rfq-details-page__inner">
        <div class="rfq-details-page__header">
            <div class="rfq-details-page__copy">
                <h1><?= $block->escapeHtml(__('Quotation Request Details')) ?></h1>
                <p><?= $block->escapeHtml(__('Review the supplier, requested items, and customer message for this RFQ.')) ?></p>
            </div>

            <button type="button" class="rfq-details__pdf-btn" onclick="window.print()">
                <svg viewBox="0 0 18 18" aria-hidden="true">
                    <path d="M4 2.5h6.2L14 6.3V15H4z" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linejoin="round"/>
                    <path d="M10.2 2.5V6.3H14" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linejoin="round"/>
                    <path d="M6 10h6M6 12.5h4.2" fill="none" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/>
                </svg>
                <span><?= $block->escapeHtml(__('Download PDF')) ?></span>
            </button>
        </div>

        <?php if (!$quote || !$quote->getId()): ?>
            <div class="rfq-details-empty">
                <h2><?= $block->escapeHtml(__('RFQ not found')) ?></h2>
                <p><?= $block->escapeHtml(__('This quotation request could not be loaded.')) ?></p>
                <a class="rfq-details__secondary-btn" href="<?= $block->escapeUrl($block->getHistoryUrl()) ?>">
                    <?= $block->escapeHtml(__('Back to Requests')) ?>
                </a>
            </div>
        <?php else: ?>
            <div class="rfq-details-page__content">
                <section class="rfq-details-card">
                    <div class="rfq-details-card__head">
                        <h2><?= $block->escapeHtml(__('Request Overview')) ?></h2>
                        <span class="rfq-status-pill rfq-status-pill--<?= $block->escapeHtmlAttr($statusClass) ?>">
                            <?= $block->escapeHtml($statusLabel) ?>
                        </span>
                    </div>

                    <div class="rfq-details-overview">
                        <div class="rfq-details-meta">
                            <span><?= $block->escapeHtml(__('Request ID')) ?></span>
                            <strong><?= $block->escapeHtml($block->getRequestIdLabel()) ?></strong>
                        </div>

                        <div class="rfq-details-meta">
                            <span><?= $block->escapeHtml(__('Supplier')) ?></span>
                            <strong><?= $block->escapeHtml($supplierName ?: __('N/A')) ?></strong>
                        </div>

                        <div class="rfq-details-meta">
                            <span><?= $block->escapeHtml(__('Date Created')) ?></span>
                            <strong><?= $block->escapeHtml($block->getRequestDate()) ?></strong>
                        </div>

                        <div class="rfq-details-meta">
                            <span><?= $block->escapeHtml(__('Time Created')) ?></span>
                            <strong><?= $block->escapeHtml($block->getRequestTime()) ?></strong>
                        </div>
                    </div>
                </section>

                <section class="rfq-details-card">
                    <div class="rfq-details-card__head">
                        <h2><?= $block->escapeHtml(__('Items Requested')) ?></h2>
                        <span class="rfq-details-count"><?= $block->escapeHtml(__('%1 Items', $block->getItemCount())) ?></span>
                    </div>

                    <div class="rfq-details-items">
                        <?php if (!empty($items) && count($items)): ?>
                            <?php foreach ($items as $item): ?>
                                <?php
                                    $productId = (int) $item->getProductId();
                                    $imageUrl = $block->getProductImageUrl($productId);
                                    $productName = $block->getProductName($productId);
                                    $productSku = $block->getProductSku($productId);
                                    $qty = (int) $item->getQty();
                                ?>
                                <article class="rfq-details-item">
                                    <div class="rfq-details-item__thumb">
                                        <?php if ($imageUrl !== ''): ?>
                                            <img src="<?= $block->escapeUrl($imageUrl) ?>" alt="<?= $block->escapeHtmlAttr($productName) ?>">
                                        <?php else: ?>
                                            <span><?= $block->escapeHtml(__('No Image')) ?></span>
                                        <?php endif; ?>
                                    </div>

                                    <div class="rfq-details-item__copy">
                                        <h3><?= $block->escapeHtml($productName) ?></h3>
                                        <p><?= $block->escapeHtml(__('SKU: %1', $productSku)) ?></p>
                                    </div>

                                    <div class="rfq-details-item__qty">
                                        <span><?= $block->escapeHtml(__('Quantity')) ?></span>
                                        <strong><?= $block->escapeHtml((string) $qty) ?></strong>
                                    </div>
                                </article>
                            <?php endforeach; ?>
                        <?php else: ?>
                            <div class="rfq-details-empty-items">
                                <?= $block->escapeHtml(__('No products were found for this RFQ.')) ?>
                            </div>
                        <?php endif; ?>
                    </div>
                </section>

                <section class="rfq-details-card">
                    <div class="rfq-details-card__head">
                        <h2><?= $block->escapeHtml(__('Customer Message')) ?></h2>
                    </div>

                    <div class="rfq-details-message">
                        <?php if ($message !== ''): ?>
                            <p><?= nl2br($block->escapeHtml($message)) ?></p>
                        <?php else: ?>
                            <p><?= $block->escapeHtml(__('No customer message was provided.')) ?></p>
                        <?php endif; ?>
                    </div>
                </section>

                <div class="rfq-details-actions">
                    <?php if ($isCancelable): ?>
                        <form class="rfq-details-actions__form" action="<?= $block->escapeUrl($block->getCancelUrl()) ?>" method="post">
                            <input type="hidden" name="form_key" value="<?= $block->escapeHtmlAttr($block->getFormKey()) ?>">
                            <input type="hidden" name="quote_id" value="<?= $block->escapeHtmlAttr($quoteId) ?>">
                            <button type="submit" class="rfq-details__cancel-btn">
                                <?= $block->escapeHtml(__('Cancel Request')) ?>
                            </button>
                        </form>
                    <?php else: ?>
                        <button type="button" class="rfq-details__cancel-btn is-disabled" disabled>
                            <?= $block->escapeHtml(__('Cancel Request')) ?>
                        </button>
                    <?php endif; ?>

                    <a class="rfq-details__rerequest-btn" href="<?= $block->escapeUrl($block->getReRequestUrl()) ?>">
                        <?= $block->escapeHtml(__('Re-Request Quotation')) ?>
                    </a>
                </div>
            </div>
        <?php endif; ?>
    </div>
</div>

<style>
.rfq-details-page {
    width: 100%;
    background: linear-gradient(180deg, #f7f9fe 0%, #f3f6fb 100%);
    color: #16233f;
}

.rfq-details-page,
.rfq-details-page * {
    box-sizing: border-box;
}

.rfq-details-page__inner {
    max-width: 1184px;
    margin: 0 auto;
    padding: 104px 24px 72px;
}

.rfq-details-page__header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 20px;
    margin-bottom: 28px;
}

.rfq-details-page__copy {
    max-width: 760px;
}

.rfq-details-page__copy h1 {
    margin: 0;
    color: #1f3d72;
    font-size: clamp(34px, 4vw, 58px);
    line-height: 1.05;
    letter-spacing: -0.03em;
    font-weight: 800;
}

.rfq-details-page__copy p {
    margin: 10px 0 0;
    color: #6b7890;
    font-size: clamp(15px, 1.5vw, 18px);
    line-height: 1.55;
}

.rfq-details-page__content {
    width: min(100%, 780px);
}

.rfq-details__pdf-btn,
.rfq-details__secondary-btn,
.rfq-details__cancel-btn,
.rfq-details__rerequest-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    min-height: 48px;
    padding: 12px 18px;
    border-radius: 11px;
    border: 1px solid transparent;
    font-size: 14px;
    font-weight: 700;
    text-decoration: none;
    transition: transform .18s ease, box-shadow .18s ease, background-color .18s ease, border-color .18s ease, color .18s ease;
}

.rfq-details__pdf-btn:hover,
.rfq-details__secondary-btn:hover,
.rfq-details__cancel-btn:hover,
.rfq-details__rerequest-btn:hover {
    transform: translateY(-1px);
}

.rfq-details__pdf-btn {
    background: #fff;
    color: #1f3d72;
    border-color: #d5ddea;
    box-shadow: 0 8px 20px rgba(31, 61, 114, 0.06);
    white-space: nowrap;
}

.rfq-details__pdf-btn svg {
    width: 18px;
    height: 18px;
    flex: 0 0 18px;
}

.rfq-details-empty {
    width: min(100%, 780px);
    padding: 32px;
    border: 1px solid #e5e7eb;
    border-radius: 18px;
    background: #fff;
    box-shadow: 0 10px 26px rgba(31, 61, 114, 0.06);
}

.rfq-details-empty h2 {
    margin: 0 0 8px;
    color: #1f3d72;
    font-size: 24px;
}

.rfq-details-empty p {
    margin: 0 0 18px;
    color: #6b7890;
}

.rfq-details__secondary-btn {
    color: #1f3d72;
    border-color: #d5ddea;
    background: #fff;
}

.rfq-details-card {
    margin-bottom: 22px;
    background: #fff;
    border: 1px solid #dfe6f1;
    border-radius: 16px;
    box-shadow: 0 10px 26px rgba(31, 61, 114, 0.06);
    overflow: hidden;
}

.rfq-details-card__head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    padding: 18px 20px;
    border-bottom: 1px solid #e9edf5;
}

.rfq-details-card__head h2 {
    margin: 0;
    color: #1f2f4d;
    font-size: 18px;
    line-height: 1.2;
    font-weight: 700;
}

.rfq-status-pill {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 36px;
    padding: 0 14px;
    border-radius: 999px;
    border: 1px solid transparent;
    font-size: 13px;
    font-weight: 700;
    white-space: nowrap;
}

.rfq-status-pill--pending {
    color: #c2410c;
    background: #fff7ed;
    border-color: #fdc88b;
}

.rfq-status-pill--approved {
    color: #166534;
    background: #f0fdf4;
    border-color: #b7ebc6;
}

.rfq-status-pill--rejected {
    color: #991b1b;
    background: #fef2f2;
    border-color: #f5b9b9;
}

.rfq-status-pill--cancelled,
.rfq-status-pill--default {
    color: #475569;
    background: #f8fafc;
    border-color: #d7e0eb;
}

.rfq-details-overview {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 20px 28px;
    padding: 20px;
}

.rfq-details-meta {
    display: flex;
    flex-direction: column;
    gap: 6px;
}

.rfq-details-meta span {
    color: #8b97ae;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: .05em;
    text-transform: uppercase;
}

.rfq-details-meta strong {
    color: #1f2f4d;
    font-size: 17px;
    line-height: 1.35;
}

.rfq-details-count {
    display: inline-flex;
    align-items: center;
    min-height: 28px;
    padding: 0 10px;
    border-radius: 8px;
    background: #eff4fb;
    color: #6b7890;
    font-size: 12px;
    font-weight: 700;
}

.rfq-details-items {
    padding: 4px 0;
}

.rfq-details-item {
    display: grid;
    grid-template-columns: 72px minmax(0, 1fr) 108px;
    gap: 16px;
    align-items: center;
    padding: 18px 20px;
    border-top: 1px solid #edf1f7;
}

.rfq-details-item:first-child {
    border-top: 0;
}

.rfq-details-item__thumb {
    width: 72px;
    height: 72px;
    border-radius: 10px;
    border: 1px solid #e5eaf3;
    background: #f8fafc;
    overflow: hidden;
    display: grid;
    place-items: center;
}

.rfq-details-item__thumb img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.rfq-details-item__thumb span {
    color: #94a3b8;
    font-size: 11px;
    font-weight: 700;
    text-transform: uppercase;
    text-align: center;
    padding: 0 8px;
}

.rfq-details-item__copy {
    min-width: 0;
}

.rfq-details-item__copy h3 {
    margin: 0;
    color: #1f2f4d;
    font-size: 17px;
    line-height: 1.35;
    font-weight: 700;
}

.rfq-details-item__copy p {
    margin: 6px 0 0;
    color: #6b7890;
    font-size: 13px;
    line-height: 1.4;
}

.rfq-details-item__qty {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 6px;
}

.rfq-details-item__qty span {
    color: #8b97ae;
    font-size: 12px;
    font-weight: 700;
    letter-spacing: .08em;
    text-transform: uppercase;
}

.rfq-details-item__qty strong {
    color: #1f2f4d;
    font-size: 22px;
    line-height: 1;
    font-weight: 800;
}

.rfq-details-empty-items {
    padding: 20px;
    color: #6b7890;
    font-size: 14px;
}

.rfq-details-message {
    padding: 22px 20px 24px;
}

.rfq-details-message p {
    margin: 0;
    padding: 16px 18px;
    border-radius: 10px;
    border: 1px solid #cfdcf4;
    background: #f6f8ff;
    color: #54627d;
    font-size: 15px;
    line-height: 1.7;
}

.rfq-details-actions {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 16px;
    margin-top: 26px;
}

.rfq-details-actions__form {
    margin: 0;
}

.rfq-details__cancel-btn {
    min-width: 132px;
    background: #f1f5f9;
    color: #334155;
    border-color: #e2e8f0;
}

.rfq-details__cancel-btn.is-disabled {
    cursor: not-allowed;
    opacity: .75;
}

.rfq-details__rerequest-btn {
    min-width: 176px;
    background: #fff;
    color: #1f3d72;
    border-color: #bfd0eb;
    box-shadow: 0 8px 20px rgba(31, 61, 114, 0.06);
}

@media (max-width: 1024px) {
    .rfq-details-page__inner {
        padding-top: 92px;
    }

    .rfq-details-page__content,
    .rfq-details-empty {
        width: 100%;
    }
}

@media (max-width: 767px) {
    .rfq-details-page__inner {
        padding: 88px 16px 56px;
    }

    .rfq-details-page__header {
        flex-direction: column;
        align-items: flex-start;
    }

    .rfq-details-page__copy {
        max-width: 100%;
    }

    .rfq-details__pdf-btn {
        width: 100%;
    }

    .rfq-details-card__head {
        padding: 16px;
    }

    .rfq-details-overview {
        grid-template-columns: 1fr;
        gap: 16px;
        padding: 16px;
    }

    .rfq-details-item {
        grid-template-columns: 56px minmax(0, 1fr);
        gap: 14px;
        padding: 16px;
    }

    .rfq-details-item__qty {
        grid-column: 2 / -1;
        align-items: flex-start;
        margin-top: 4px;
    }

    .rfq-details-message {
        padding: 16px;
    }

    .rfq-details-actions {
        flex-direction: column;
        align-items: stretch;
    }

    .rfq-details-actions__form,
    .rfq-details__cancel-btn,
    .rfq-details__rerequest-btn {
        width: 100%;
    }
}

@media print {
    .custom-page-header,
    .rfq-details-page__header button,
    .rfq-details-actions {
        display: none !important;
    }

    .rfq-details-page {
        background: #fff !important;
    }

    .rfq-details-page__inner {
        padding-top: 0;
    }

    .rfq-details-card,
    .rfq-details-empty {
        box-shadow: none !important;
    }
}
</style>
