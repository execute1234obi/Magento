<?php
/** @var \Vendor\QuoteRequest\Block\View $block */
$items = $block->getQuoteItems();
$imageHelper = $this->helper(\Magento\Catalog\Helper\Image::class);
?>

<div class="gcc-quote-page">
    <div class="gcc-container">
        <form id="quote-form-validate" 
              action="<?= $block->getUrl('quoterequest/index/submit') ?>" 
              method="post" 
              data-mage-init='{"validation":{}}'>
            
            <div class="quote-page">

                <div class="quote-breadcrumb">
                    <a href="<?= $block->getUrl('') ?>"><?= __('Home') ?></a>
                    <span class="sep">/</span>
                    <span><?= __('Quotation Submission Page') ?></span>
                </div>

                <h1 class="quote-title"><?= __('Choose your location') ?></h1>

                <div class="quote-card quote-card--location">
                    <div class="quote-grid-2">
                        <div class="quote-field">
                            <label for="country" class="label"><span><?= __('Country') ?></span></label>
                            <select id="country" name="country_id" title="Country" class="quote-select" data-validate="{'validate-select':true}">
                                <option value=""><?= __('Select Country') ?></option>
                                <?php foreach ($block->getCountryCollection() as $country): ?>
                                    <option value="<?= $country->getCountryId() ?>">
                                        <?= $country->getName() ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <div class="quote-field">
                            <label for="region_id" class="label"><span><?= __('State/Province') ?></span></label>
                            <select id="region_id" name="region_id" title="State" class="quote-select" style="display:none;" data-validate="{'validate-select':true}">
                                <option value=""><?= __('Select State/Region') ?></option>
                            </select>
                            <input type="text" id="region" name="region" title="State" class="quote-select" style="display:none;" />
                        </div>
                    </div>
                </div>

                <div class="quote-card">
                    <div class="quote-card-head">
                        <div class="quote-card-title">
                            <h2><?= __('Quotation List') ?></h2>
                            <span><?= __('(%1 products selected)', count($items)) ?></span>
                        </div>
                    </div>

                    <?php if (!empty($items)): ?>
                        <label class="quote-select-all">
                            <input type="checkbox" class="quote-checkbox" id="select-all-products" checked />
                            <span><?= __('Select All Product') ?></span>
                        </label>

                        <?php foreach ($items as $item): ?>
                            <?php 
                                $_product = $item['product']; 
                                $productImage = $imageHelper->init($_product, 'product_thumbnail_image')->getUrl();
                                $vendorName = $_product->getAttributeText('vendor_id') ?: 'General Vendor';
                            ?>
                            <div class="vendor-block" id="product-block-<?= $_product->getId() ?>">
                                <div class="vendor-head">
                                    <div class="vendor-left">
                                        <div class="vendor-logo"><span>⌁</span></div>
                                        <div class="vendor-name">
                                            <strong><?= $block->escapeHtml($vendorName) ?></strong>
                                            <span class="vendor-verified" title="Verified">✔</span>
                                        </div>
                                    </div>
                                    <button class="vendor-trash" type="button" title="Remove vendor" data-id="<?= $_product->getId() ?>">🗑</button>
                                </div>
                                
                                <div class="product-row">
                                    <div class="product-check">
                                        <input type="checkbox" name="selected_products[]" value="<?= $_product->getId() ?>" class="quote-checkbox product-item-checkbox" checked />
                                    </div>

                                    <div class="product-thumb">
                                        <img src="<?= $productImage ?>" alt="<?= $block->escapeHtml($_product->getName()) ?>" />
                                    </div>

                                    <div class="product-info">
                                        <h3 class="product-title"><?= $block->escapeHtml($_product->getName()) ?></h3>
                                        <div class="product-desc">
                                            <div class="desc-label"><?= __('SKU:') ?> <?= $block->escapeHtml($_product->getSku()) ?></div>
                                            <p><?= /* @noEscape */ $block->escapeHtml(strip_tags($_product->getShortDescription())) ?></p>
                                        </div>
                                    </div>

                                    <div class="product-qty">
                                        <div class="qty-stepper">
                                            <button type="button" class="qty-btn minus">−</button>
                                            <input type="text" name="qty[<?= $_product->getId() ?>]" value="<?= $item['qty'] ?>" class="qty-input" readonly />
                                            <button type="button" class="qty-btn plus">+</button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <?php endforeach; ?>
                        
                    <?php else: ?>
                        <div class="no-products">
                            <p><?= __('Your quotation list is empty.') ?></p>
                        </div>
                    <?php endif; ?>

                    <div class="draft-inquiry">
                        <div class="draft-head">
                            <h3><?= __('Draft inquiry') ?></h3>
                            <span><?= __('(for the selected product vendors)') ?></span>
                        </div>
                        <textarea name="customer_note" class="quote-textarea" rows="4" placeholder="<?= __('Write your message here...') ?>" data-validate="{required:true}"></textarea>
                    </div>

                    <div class="quote-bottom-bar">
                        <div class="bottom-left">
                            <strong><?= __('Quotation Request Summary') ?></strong>
                        </div>

                        <div class="bottom-right">
                            <span class="product-count"><?= count($items) ?> <?= __('Products') ?></span>
                            <button class="quote-primary-btn" type="submit" id="submit-quote">
                                <?= __('Confirm Quotation Requests') ?>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>

<style>
/* Previous Styles Kept Intact */
.gcc-quote-page{ background: #f6f8fb; padding-top: 103px; padding-bottom: 50px;}
.gcc-container{ width: min(1200px, calc(100% - 48px)); margin: 0 auto; }
.quote-page{ font-family: system-ui, -apple-system, sans-serif; color: #111827; }
.quote-breadcrumb{ font-size: 13px; color: #6b7280; margin: 0 0 14px; }
.quote-breadcrumb a{ color:#6b7280; text-decoration:none; }
.quote-title{ font-size: 22px; font-weight: 650; margin: 0 0 14px; }
.quote-card{ background: #fff; border: 1px solid #e5e7eb; border-radius: 12px; padding: 18px; margin-top: 14px; }
.quote-grid-2{ display:grid; grid-template-columns: 1fr 1fr; gap: 16px; }
.quote-select{ width: 100%; height: 48px; border: 1px solid #d1d5db; border-radius: 10px; padding: 0 14px; font-size: 14px; }
.vendor-block{ border: 1px solid #eef2f7; border-radius: 12px; padding: 14px; margin-bottom: 14px; }
.vendor-head{ display:flex; align-items:center; justify-content:space-between; border-bottom: 1px solid #eef2f7; margin-bottom: 14px; padding-bottom: 10px;}
.product-row{ display:grid; grid-template-columns: 32px 180px 1fr 160px; gap: 16px; align-items:center; }
.product-thumb{ width: 180px; height: 130px; border-radius: 12px; background:#f3f4f6; display:grid; place-items:center; overflow:hidden; }
.product-thumb img{ width:100%; height:100%; object-fit:contain; }
.qty-stepper{ display:grid; grid-template-columns: 42px 58px 42px; border: 1px solid #e5e7eb; border-radius: 10px; overflow:hidden; height: 40px; }
.qty-btn{ border:none; background:#fff; cursor:pointer; font-size: 18px; }
.qty-input{ border:none; text-align:center; font-size: 14px; font-weight: 650; width: 100%; }
.quote-textarea{ width:100%; border: 1px solid #d1d5db; border-radius: 10px; padding: 12px; min-height: 110px; }
.quote-primary-btn{ height: 44px; padding: 0 24px; border:none; border-radius: 10px; background:#0f2f5f; color:#fff; font-weight:650; cursor:pointer; }
.mage-error{ color: #e02b27; font-size: 12px; margin-top: 5px; display: block; }
/* Responsive CSS included as per previous version */
@media (max-width: 992px){
    .product-row{ grid-template-columns: 32px 140px 1fr; }
    .product-qty{ grid-column: 2 / -1; justify-content: flex-start; }
}
</style>

<script type="text/x-magento-init">
    {
        "#country": {
            "regionUpdater": {
                "optionalRegionAllowed": true,
                "regionListId": "#region_id",
                "regionInputId": "#region",
                "postcodeId": "#zip",
                "form": "#quote-form-validate",
                "regionJson": <?= /* @noEscape */ $block->getRegionJson() ?>,
                "defaultRegion": "0",
                "countriesWithOptionalZip": <?= /* @noEscape */ $this->helper(\Magento\Directory\Helper\Data::class)->getCountriesWithOptionalZip(true) ?>
            }
        }
    }
</script>

<script>
require(['jquery', 'mage/validation'], function($){
    $(document).ready(function(){
        
        // 1. Quantity Plus
        $(document).on('click', '.qty-btn.plus', function(){
            var input = $(this).siblings('.qty-input');
            var val = parseInt(input.val());
            input.val(val + 1);
        });

        // 2. Quantity Minus
        $(document).on('click', '.qty-btn.minus', function(){
            var input = $(this).siblings('.qty-input');
            var val = parseInt(input.val());
            if(val > 1) input.val(val - 1);
        });

        // 3. Trash/Remove Logic
        $(document).on('click', '.vendor-trash', function(){
            var productId = $(this).data('id');
            var removeUrl = '<?= $block->getUrl("quoterequest/index/remove") ?>' + 'id/' + productId;
            if(confirm('<?= __("Remove this product from list?") ?>')) {
                window.location.href = removeUrl;
            }
        });

        // 4. Select All Checkbox
        $('#select-all-products').on('change', function(){
            $('.product-item-checkbox').prop('checked', $(this).prop('checked'));
        });

        // 5. Form Validation on Submit
        $('#quote-form-validate').on('submit', function(e){
            if(!$(this).valid()){
                return false;
            }
            // Check if at least one product is selected
            if($('.product-item-checkbox:checked').length === 0){
                alert('<?= __("Please select at least one product.") ?>');
                return false;
            }
        });
    });
});
</script>