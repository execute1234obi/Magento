<?php
$profileHtml = $block->getChildHtml('myaccount_left');
$formHtml = $block->getChildHtml('myaccount_content');

$tabsHtml = '';
$tabsRenderer = $block->getLayout()->getBlock('tabs_field');
if ($tabsRenderer) {
    $tabsBlock = $tabsRenderer->getChildBlock('tabs');
    if ($tabsBlock) {
        $tabsBlock->setTemplate('Vnecoms_Vendors::widget/tabshoriz.phtml');
        $tabsHtml = $tabsBlock->toHtml();
    } else {
        $tabsHtml = $tabsRenderer->getChildHtml();
    }
}

if ($tabsHtml === '') {
    $tabsBlock = $block->getLayout()->getBlock('tabs');
    if ($tabsBlock) {
        $tabsBlock->setTemplate('Vnecoms_Vendors::widget/tabshoriz.phtml');
        $tabsHtml = $tabsBlock->toHtml();
    }
}

$sectionTitle = __('Business Contact');
?>

<div class="vendor-account-page">
    <div class="vendor-account-page__head">
        <h1 class="vendor-account-page__title">
            <?= $block->escapeHtml(__('Vendor Account')) ?>
        </h1>
        <div class="vendor-account-page__actions">
            <button type="button"
                    class="vendor-account-page__save"
                    data-mage-init='{"button":{"event":"save","target":"#edit_form"}}'>
                <span><?= $block->escapeHtml(__('Save Account')) ?></span>
            </button>
        </div>
    </div>

    <div class="vendor-account-layout">
        <aside class="vendor-account-sidebar">
            <div class="vendor-account-card vendor-account-summary">
                <?= /* @noEscape */ $profileHtml ?>
            </div>

            <?php if ($tabsHtml): ?>
                <div class="vendor-account-card vendor-account-menu">
                    <div class="vendor-account-card__heading">
                        <?= $block->escapeHtml(__('User Information')) ?>
                    </div>
                    <div class="vendor-account-menu__body">
                        <?= /* @noEscape */ $tabsHtml ?>
                    </div>
                </div>
            <?php endif; ?>
        </aside>

        <section class="vendor-account-main">
            <div class="vendor-account-card vendor-account-form">
                <div class="vendor-account-form__heading">
                    <span class="vendor-account-form__title" data-section-title>
                        <?= $block->escapeHtml($sectionTitle) ?>
                    </span>
                    <span class="vendor-account-form__chevron" aria-hidden="true">
                        <i class="fa fa-chevron-down" aria-hidden="true"></i>
                    </span>
                </div>
                <?= /* @noEscape */ $formHtml ?>
            </div>
        </section>
    </div>
</div>

<style>
.vendor-account-page {
  --vendor-accent: #2f5cab;
  --vendor-accent-soft: #edf3ff;
  --vendor-border: #d9e4f1;
  --vendor-card: #ffffff;
  --vendor-panel: #f8fbff;
  --vendor-text: #1f3563;
  --vendor-muted: #5b6f92;
  background: #ffffff;
  padding: 12px 0 24px;
}

.vendor-account-page__head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  margin-bottom: 14px;
}

.vendor-account-page__title {
  margin: 0;
  flex: 1 1 auto;
  color: var(--vendor-text);
  font-size: 22px;
  line-height: 1.2;
  font-weight: 700;
}

.vendor-account-page__actions {
  flex: 0 0 auto;
}

.vendor-account-page__save {
  appearance: none;
  border: 1px solid var(--vendor-accent);
  background: var(--vendor-accent);
  color: #ffffff;
  border-radius: 8px;
  padding: 11px 18px;
  font-size: 15px;
  font-weight: 700;
  line-height: 1;
  cursor: pointer;
  box-shadow: 0 1px 2px rgba(20, 37, 74, 0.12);
  transition: background-color 0.2s ease, border-color 0.2s ease, transform 0.2s ease;
}

.vendor-account-page__save:hover {
  background: #244b92;
  border-color: #244b92;
}

.vendor-account-layout {
  display: grid;
  grid-template-columns: minmax(360px, 400px) minmax(0, 1fr);
  gap: 18px;
  align-items: start;
}

.vendor-account-sidebar {
  display: flex;
  flex-direction: column;
  gap: 12px;
  min-width: 0;
}

.vendor-account-main {
  min-width: 0;
}

.vendor-account-card {
  background: var(--vendor-card);
  border: 1px solid var(--vendor-border);
  border-radius: 8px;
  box-shadow: 0 1px 2px rgba(20, 37, 74, 0.04);
  overflow: hidden;
}

.vendor-account-card__heading {
  padding: 16px 18px 12px;
  border-bottom: 1px solid #e6edf7;
  background: #ffffff;
  color: var(--vendor-text);
  font-size: 18px;
  font-weight: 700;
}

.vendor-account-summary .box.box-primary {
  margin: 0;
  border: 0;
  border-radius: 0;
  box-shadow: none;
  background: transparent;
}

.vendor-account-summary .box-body.box-profile {
  position: relative;
  padding: 18px 18px 16px;
  display: grid;
  grid-template-columns: 108px minmax(0, 1fr);
  gap: 12px 18px;
  align-items: center;
  text-align: left;
}

.vendor-account-summary .box-body.box-profile::after {
  content: "";
  position: absolute;
  top: 14px;
  bottom: 14px;
  left: 108px;
  width: 1px;
  background: #e1e8f1;
}

.vendor-account-summary .profile-user-img {
  width: 64px;
  height: 64px;
  object-fit: cover;
  grid-column: 1;
  grid-row: 1;
  justify-self: center;
  margin: 0;
  border: 0;
  border-radius: 50%;
  background: #ffffff;
  box-shadow: 0 8px 18px rgba(17, 38, 77, 0.08);
}

.vendor-account-summary .profile-username {
  grid-column: 1;
  grid-row: 2;
  justify-self: center;
  margin: 0;
  color: var(--vendor-text);
  font-size: 16px;
  line-height: 1.25;
  font-weight: 700;
  text-align: center;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  width: 100%;
}

.vendor-account-summary .vendor-profile-meta {
  color: var(--vendor-muted);
  font-size: 14px;
  line-height: 1.45;
  display: flex;
  flex-direction: column;
  gap: 8px;
  padding-top: 2px;
  min-width: 0;
  position: relative;
}

.vendor-account-summary .vendor-profile-meta--joined {
  grid-column: 2;
  grid-row: 1;
}

.vendor-account-summary .vendor-profile-meta--location {
  grid-column: 2;
  grid-row: 2;
}

.vendor-account-summary .vendor-profile-meta strong {
  color: var(--vendor-text);
  font-weight: 600;
  display: flex;
  align-items: flex-start;
  gap: 8px;
}

.vendor-account-summary .vendor-profile-meta__line {
  display: flex;
  align-items: center;
  gap: 8px;
  white-space: nowrap;
}

.vendor-account-summary .vendor-profile-meta--joined .vendor-profile-meta__line {
  color: var(--vendor-text);
}

.vendor-account-summary .vendor-profile-meta--location .vendor-profile-meta__line {
  align-items: center;
  gap: 8px;
}

.vendor-account-summary .vendor-profile-meta i {
  color: var(--vendor-accent);
  margin-top: 0;
  flex: 0 0 auto;
}

.vendor-account-summary .vendor-profile-meta .text-muted {
  color: var(--vendor-muted);
  display: block;
  margin-top: 4px;
  overflow-wrap: anywhere;
}

.vendor-account-summary .vendor-profile-meta--location .text-muted {
  display: inline;
  margin-top: 0;
  line-height: 1.45;
  white-space: nowrap;
}

.vendor-account-summary .text-muted.text-center {
  display: none;
}

.vendor-account-summary .vendor-profile-meta__line {
  display: flex;
  align-items: flex-start;
  gap: 8px;
}

.vendor-account-summary .vendor-profile-meta--joined .vendor-profile-meta__line {
  color: var(--vendor-text);
  white-space: nowrap;
}

.vendor-account-summary .vendor-profile-meta--location {
  gap: 0;
}

.vendor-account-summary .vendor-profile-meta--location .vendor-profile-meta__line {
  align-items: center;
  gap: 8px;
}

.vendor-account-summary .vendor-profile-meta--location .text-muted {
  display: inline;
  margin-top: 0;
  line-height: 1.45;
}

.vendor-account-summary .vendor-profile-meta--location i {
  margin-top: 0;
}

.vendor-account-summary .box-body.box-profile hr,
.vendor-account-summary .box-body.box-profile br {
  display: none;
}

.vendor-account-menu__body {
  padding: 12px 16px 18px;
  background: #f8fbff;
}

.vendor-account-tabs {
  margin: 0;
}

.vendor-account-tabs__list,
.vendor-account-menu .nav.nav-tabs {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 20px;
  border: 0;
  background: transparent;
}

.vendor-account-menu .nav.nav-tabs {
  border-bottom: 0 !important;
}

.vendor-account-tabs__list {
  border: 0;
}

.vendor-account-tabs__item,
.vendor-account-menu .nav.nav-tabs > li {
  position: relative;
  margin: 0;
  float: none;
  width: 100%;
  overflow: hidden;
  background: transparent;
  min-height: 52px;
}

.vendor-account-tabs__item::before,
.vendor-account-menu .nav.nav-tabs > li::before {
  content: "";
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 6px;
  background: transparent;
  transition: background-color 0.2s ease;
}

.vendor-account-tabs__item.is-hidden {
  display: none;
}

.vendor-account-tabs__link,
.vendor-account-menu .nav.nav-tabs > li > a {
  display: block;
  width: 100%;
  padding: 16px 16px 16px 22px;
  background: transparent;
  color: var(--vendor-text);
  font-size: 16px;
  font-weight: 700;
  line-height: 1.35;
  border: 0 !important;
  border-radius: 0 !important;
  text-decoration: none;
  box-shadow: none !important;
  transition: background-color 0.2s ease, border-color 0.2s ease, color 0.2s ease;
}

.vendor-account-tabs__link:hover,
.vendor-account-menu .nav.nav-tabs > li > a:hover {
  background: var(--vendor-accent-soft) !important;
  color: var(--vendor-accent) !important;
}

.vendor-account-tabs__item.ui-state-active,
.vendor-account-tabs__item.ui-tabs-active,
.vendor-account-tabs__item.active,
.vendor-account-tabs__item[aria-selected="true"],
.vendor-account-menu .nav.nav-tabs > li.active {
  background: var(--vendor-accent-soft) !important;
  box-shadow: inset 6px 0 0 var(--vendor-accent);
}

.vendor-account-tabs__item.ui-state-active::before,
.vendor-account-tabs__item.ui-tabs-active::before,
.vendor-account-tabs__item.active::before,
.vendor-account-tabs__item[aria-selected="true"]::before,
.vendor-account-menu .nav.nav-tabs > li.active::before,
.vendor-account-menu .nav.nav-tabs > li[aria-selected="true"]::before {
  background: var(--vendor-accent);
}

.vendor-account-tabs__item.ui-state-active > .vendor-account-tabs__link,
.vendor-account-tabs__item.ui-tabs-active > .vendor-account-tabs__link,
.vendor-account-tabs__item.active > .vendor-account-tabs__link,
.vendor-account-tabs__item[aria-selected="true"] > .vendor-account-tabs__link,
.vendor-account-menu .nav.nav-tabs > li.active > a,
.vendor-account-menu .nav.nav-tabs > li.active > a:hover,
.vendor-account-menu .nav.nav-tabs > li.active > a:focus,
.vendor-account-menu .nav.nav-tabs > li[aria-selected="true"] > a {
  background: transparent !important;
  color: var(--vendor-accent) !important;
}

.vendor-account-tabs [data-role="item-messages"] {
  display: none;
}

.vendor-account-tabs__item > div {
  display: none !important;
}

.vendor-account-main .vendor-account-form {
  padding: 20px;
}

.vendor-account-main .vendor-account-form__heading {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding: 0 0 14px;
  margin-bottom: 18px;
  border-bottom: 1px solid #edf2fa;
  color: var(--vendor-text);
  font-size: 20px;
  font-weight: 700;
  line-height: 1.2;
}

.vendor-account-main .vendor-account-form__title {
  display: block;
  min-width: 0;
}

.vendor-account-main .vendor-account-form__chevron {
  flex: 0 0 auto;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 24px;
  height: 24px;
  color: var(--vendor-text);
  font-size: 16px;
  line-height: 1;
}

.vendor-account-main .entry-edit {
  margin: 0;
}

.vendor-account-main #vendor_info_tab_content {
  margin-bottom: 18px;
}

.vendor-account-main .fieldset,
.vendor-account-main .panel,
.vendor-account-main .block {
  border-color: var(--vendor-border);
  box-shadow: none;
}

.vendor-account-main .legend,
.vendor-account-main .section-title,
.vendor-account-main .panel-heading {
  background: transparent;
  border-bottom: 1px solid #edf2fa;
  color: var(--vendor-text);
  font-size: 20px;
  font-weight: 700;
  margin: 0 0 16px;
  padding: 12px 0 14px;
}

.vendor-account-main .form-group {
  margin-bottom: 14px;
}

.vendor-account-main .row {
  margin-left: -8px;
  margin-right: -8px;
}

.vendor-account-main .row > [class*="col-"] {
  padding-left: 8px;
  padding-right: 8px;
}

.vendor-account-main .form-control,
.vendor-account-main input[type="text"],
.vendor-account-main input[type="email"],
.vendor-account-main input[type="tel"],
.vendor-account-main input[type="number"],
.vendor-account-main input[type="url"],
.vendor-account-main select,
.vendor-account-main textarea {
  width: 100%;
  height: 42px;
  padding: 10px 12px;
  border: 1px solid #d9e2f1;
  border-radius: 6px;
  background: #fff;
  color: #2c3e5d;
  box-shadow: none;
  box-sizing: border-box;
}

.vendor-account-main textarea {
  height: auto;
  min-height: 130px;
  resize: vertical;
}

.vendor-account-main .form-control:focus,
.vendor-account-main input:focus,
.vendor-account-main select:focus,
.vendor-account-main textarea:focus {
  border-color: var(--vendor-accent);
  box-shadow: 0 0 0 3px rgba(47, 92, 171, 0.12);
  outline: none;
}

.vendor-account-main .note-editor,
.vendor-account-main .tox-tinymce {
  border: 1px solid #d9e2f1 !important;
  border-radius: 6px;
  overflow: hidden;
}

.vendor-account-main .page-form-actions {
  display: none !important;
}

.vendor-account-main .page-form-actions .save.primary,
.vendor-account-main .page-form-actions .action-default.scalable.save.primary {
  display: none !important;
}

.vendor-account-main .page-form-actions .btn,
.vendor-account-main .page-form-actions button,
.vendor-account-main .action.save,
.vendor-account-main .save.button {
  border-radius: 8px;
  box-shadow: none;
}

.vendor-account-main .action.save,
.vendor-account-main .btn-primary,
.vendor-account-main button[type="submit"] {
  background: var(--vendor-accent);
  border-color: var(--vendor-accent);
  color: #fff;
}

.vendor-account-main .action.save:hover,
.vendor-account-main .btn-primary:hover,
.vendor-account-main button[type="submit"]:hover {
  background: #244b92;
  border-color: #244b92;
  color: #fff;
}

.vendor-account-main .btn-warning,
.vendor-account-main .btn-github {
  display: none !important;
}

.vendor-account-main .vendor-upload-field {
  margin-bottom: 24px;
}

.vendor-account-main .vendor-upload-field > .label.admin__field-label {
  display: block !important;
  float: none !important;
  width: 100% !important;
  margin: 0 0 12px;
  padding: 0;
  color: var(--vendor-text);
  font-size: 16px;
  font-weight: 700;
  line-height: 1.3;
  text-align: left !important;
  direction: ltr !important;
  margin-right: auto !important;
}

.vendor-account-main .vendor-upload-field > .label.admin__field-label span {
  display: block;
  width: 100%;
  text-align: left;
}

.vendor-account-main .vendor-upload-field__dropzone {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 12px;
  min-height: 180px;
  padding: 28px 22px 26px;
  border: 1px dashed #d8e4f3;
  border-radius: 10px;
  background: #f9fbff;
  color: var(--vendor-muted);
  text-align: center;
  cursor: pointer;
  overflow: hidden;
  transition: border-color 0.2s ease, background-color 0.2s ease, box-shadow 0.2s ease;
}

.vendor-account-main .vendor-upload-field__dropzone:hover,
.vendor-account-main .vendor-upload-field__dropzone.is-dragover {
  border-color: var(--vendor-accent);
  background: #f4f8ff;
  box-shadow: inset 0 0 0 1px rgba(47, 92, 171, 0.08);
}

.vendor-account-main .vendor-upload-field__content {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  width: 100%;
  pointer-events: none;
}

.vendor-account-main .vendor-upload-field__current {
  display: flex;
  align-items: center;
  justify-content: center;
  max-width: 100%;
}

.vendor-account-main .vendor-upload-field__current-image {
  display: block;
  max-width: 96px;
  max-height: 96px;
  object-fit: contain;
  border-radius: 10px;
  border: 1px solid #e3ebf5;
  background: #fff;
}

.vendor-account-main .vendor-upload-field__current-file {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 10px 14px;
  border: 1px solid #e3ebf5;
  border-radius: 999px;
  background: #fff;
  color: var(--vendor-text);
  font-size: 14px;
  font-weight: 600;
}

.vendor-account-main .vendor-upload-field__current-file i,
.vendor-account-main .vendor-upload-field__icon {
  color: var(--vendor-accent);
}

.vendor-account-main .vendor-upload-field__icon {
  font-size: 24px;
}

.vendor-account-main .vendor-upload-field__prompt {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
  color: #6e7fa0;
  font-size: 18px;
  line-height: 1.35;
  font-weight: 600;
}

.vendor-account-main .vendor-upload-field__button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-width: 160px;
  padding: 13px 24px;
  border: 1px solid var(--vendor-accent);
  border-radius: 9px;
  background: #ffffff;
  color: var(--vendor-accent);
  font-size: 15px;
  font-weight: 700;
  line-height: 1;
  box-shadow: 0 1px 1px rgba(20, 37, 74, 0.04);
}

.vendor-account-main .vendor-upload-field__selected {
  min-height: 18px;
  color: #6e7fa0;
  font-size: 13px;
  line-height: 1.4;
}

.vendor-account-main .vendor-upload-field__input {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  opacity: 0;
  pointer-events: none;
  cursor: pointer;
}

.vendor-account-main .vendor-upload-field__after {
  display: none;
}

@media (max-width: 991px) {
  .vendor-account-layout {
    grid-template-columns: 1fr;
  }

  .vendor-account-summary .box-body.box-profile {
    grid-template-columns: 96px minmax(0, 1fr);
    gap: 10px 14px;
  }

  .vendor-account-summary .box-body.box-profile::after {
    left: 96px;
  }

  .vendor-account-summary .profile-user-img {
    justify-self: center;
  }

  .vendor-account-summary .vendor-profile-meta {
    padding-top: 2px;
  }

  .vendor-account-main .vendor-upload-field__dropzone {
    min-height: 164px;
    padding: 22px 18px 20px;
  }

  .vendor-account-main .vendor-upload-field__prompt {
    font-size: 16px;
  }

  .vendor-account-main .vendor-upload-field__button {
    min-width: 140px;
    padding-inline: 20px;
  }
}

@media (max-width: 640px) {
  .vendor-account-page {
    padding: 10px 0 18px;
  }

  .vendor-account-page__head {
    flex-direction: column;
    align-items: stretch;
    margin-bottom: 12px;
    padding: 0 2px;
  }

  .vendor-account-page__actions {
    width: 100%;
  }

  .vendor-account-page__save {
    width: 100%;
  }

  .vendor-account-layout {
    gap: 12px;
  }

  .vendor-account-sidebar {
    gap: 12px;
  }

  .vendor-account-card {
    border-radius: 8px;
  }

  .vendor-account-card__heading {
    padding: 13px 14px 11px;
    font-size: 16px;
  }

  .vendor-account-main .vendor-account-form {
    padding: 14px;
  }

  .vendor-account-summary .box-body.box-profile {
    padding: 14px 15px;
    grid-template-columns: 80px minmax(0, 1fr);
    gap: 10px 12px;
  }

  .vendor-account-summary .box-body.box-profile::after {
    left: 80px;
    top: 12px;
    bottom: 12px;
  }

  .vendor-account-summary .profile-user-img {
    width: 56px;
    height: 56px;
  }

  .vendor-account-summary .profile-username {
    font-size: 16px;
  }

  .vendor-account-summary .vendor-profile-meta {
    font-size: 14px;
    gap: 8px;
  }

  .vendor-account-tabs__link,
  .vendor-account-menu .nav.nav-tabs > li > a {
    padding: 13px 14px 13px 15px;
    font-size: 15px;
  }
}
</style>

<script>
require(['jquery'], function ($) {
  function setSelectedName($field, fileName) {
    var $selected = $field.find('[data-upload-selected]');
    if ($selected.length) {
      $selected.text(fileName || '');
    }
  }

  function setDragState($dropzone, state) {
    $dropzone.toggleClass('is-dragover', !!state);
  }

  $(document)
    .on('change', '.vendor-upload-field__input', function () {
      var fileName = '';

      if (this.files && this.files.length) {
        fileName = this.files[0].name || '';
      } else if (this.value) {
        fileName = this.value.split('\\').pop().split('/').pop();
      }

      setSelectedName($(this).closest('.vendor-upload-field'), fileName);
    })
    .on('dragenter dragover', '[data-upload-dropzone]', function (event) {
      event.preventDefault();
      event.stopPropagation();
      setDragState($(this), true);
    })
    .on('dragleave dragend', '[data-upload-dropzone]', function (event) {
      event.preventDefault();
      event.stopPropagation();
      setDragState($(this), false);
    })
    .on('drop', '[data-upload-dropzone]', function (event) {
      var originalEvent = event.originalEvent || event;
      var dataTransfer = originalEvent.dataTransfer;
      var files = dataTransfer && dataTransfer.files ? dataTransfer.files : null;
      var input = this.querySelector('.vendor-upload-field__input');

      event.preventDefault();
      event.stopPropagation();
      setDragState($(this), false);

      if (!input || !files || !files.length) {
        return;
      }

      try {
        if (window.DataTransfer) {
          var transfer = new DataTransfer();
          Array.prototype.forEach.call(files, function (file) {
            transfer.items.add(file);
          });
          input.files = transfer.files;
        } else {
          input.files = files;
        }
      } catch (error) {
        return;
      }

      $(input).trigger('change');
    });
});
</script>
