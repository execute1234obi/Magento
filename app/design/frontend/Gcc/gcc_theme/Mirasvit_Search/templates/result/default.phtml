<?php foreach ($results as $result): ?>
    <div class="flex items-start bg-white shadow rounded-lg p-4 mb-4">
        <div class="w-16 h-16 flex-shrink-0">
            <img src="<?= $result->getThumbnailUrl(); ?>" alt="<?= $result->getTitle(); ?>" class="rounded-full object-cover">
        </div>
        <div class="ml-4 flex-grow">
            <div class="flex items-center justify-between">
                <div>
                    <h2 class="text-lg font-semibold"><?= $result->getTitle(); ?></h2>
                    <p class="text-sm text-gray-500"><?= $result->getCategory(); ?></p>
                </div>
                <div class="text-right">
                    <span class="text-sm text-gray-400"><?= $result->getCountry(); ?></span>
                    <a href="<?= $result->getUrl(); ?>" class="ml-2 inline-block px-3 py-1 bg-blue-100 text-blue-600 rounded text-sm">Visit Website</a>
                </div>
            </div>
            <p class="text-sm text-gray-600 mt-2"><?= $result->getShortDescription(); ?></p>
        </div>
    </div>
<?php endforeach; ?>
