<script type="text/html" id="vnecoms_vendors">
  <li class="mst-search__item vendor-item">
    <a class="vendor-link" data-bind="attr: { href: url }">
      <!-- COL 1: logo -->
      <span class="vendor-thumb">
        <img class="vendor-img"
             data-bind="attr: {
               src: (logo ? logo : '<?= $block->escapeUrl($block->getViewFileUrl('Magento_Catalog::images/product/placeholder/thumbnail.jpg')) ?>'),
               alt: title
             }" />
      </span>
      <!-- COL 2: text -->
      <span class="vendor-name" data-bind="text: title"></span>
      <span class="vendor-desc" data-bind="text: detail"></span>
    </a>
  </li>
</script>

<style>
    
/* ---------- 1) Make the dropdown results scrollable  ---------- */
/* Try common wrappers used by Mirasvit; whichever exists will apply */
.mst-search__autocomplete-results,
.mst-searchautocomplete__results,
.mst-searchautocomplete__wrapper {
  max-height: min(60vh, 460px) !important;   /* keep panel short */
  overflow-y: auto !important;
  overscroll-behavior: contain;              /* stop page scroll chaining */
}

/* Keep the bottom “View all results” visible while scrolling (if present) */
.mst-searchautocomplete__show-all,
.mst-search__autocomplete-show-all {
  position: sticky;
  bottom: 0;
  z-index: 2;
  background: #fff;
  border-top: 1px solid #eee;
}

/* Optional: keep section headers visible while scrolling (if present) */
.mst-searchautocomplete__section-title,
.mst-search__section-title {
  position: sticky;
  top: 0;
  z-index: 1;
  background: #fff;
}

/* ---------- 2) Vendor row as a strict 2-column grid ---------- */
/* Force grid on the anchor (override any flex from theme) */
.mst-search__autocomplete-results li.mst-search__item.vendor-item > a.vendor-link{
  display: grid !important;
  grid-template-columns: 64px 1fr;           /* image | text */
  grid-template-areas:
    "thumb name"
    "thumb desc";
  column-gap: 14px;
  row-gap: 4px;
  align-items: center;
  padding: 12px 16px;
}

/* Map grid areas */
.mst-search__autocomplete-results .vendor-item .vendor-thumb{ grid-area: thumb; }
.mst-search__autocomplete-results .vendor-item .vendor-name{  grid-area: name;  }
.mst-search__autocomplete-results .vendor-item .vendor-desc{  grid-area: desc;  }

/* Thumb box */
.mst-search__autocomplete-results .vendor-item .vendor-thumb{
  width: 64px; height: 64px;
  border: 1px solid #e5e7eb; border-radius: 6px;
  background: #fff; display: flex; align-items: center; justify-content: center;
  overflow: hidden;
}
.mst-search__autocomplete-results .vendor-item .vendor-img{
  max-width: 100%; max-height: 100%;
  object-fit: contain; display: block;
}

/* Text styling */
.mst-search__autocomplete-results .vendor-item .vendor-name{
  font-weight: 600; line-height: 1.25; color: #1f2937; min-width: 0;
}
.mst-search__autocomplete-results .vendor-item .vendor-desc{
  color: #6b7280; line-height: 1.35; min-width: 0;
  overflow: hidden; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical;
}

/* Hover (optional) */
.mst-search__autocomplete-results .vendor-item .vendor-link:hover{ background: #f9fafb; }

/* Small screens: slightly smaller image */
@media (max-width: 480px){
  .mst-search__autocomplete-results li.mst-search__item.vendor-item > a.vendor-link{
    grid-template-columns: 56px 1fr;
  }
}
</style>
